# Source audit

Proposition 3.3(ii) quantifies over all weights and all activations satisfying global constants `C1`, `C2`, and `C3`; it does not require monotonicity or a named activation. Equation (31) prints `H0=(lambda1+lambda2)+Bres(f*+1)` and an `H1` containing all quadratic and mixed constants.

The preceding derivation has a `Clinear*f(W)` term. Its decomposition requires `Clinear*f*` in `H0`, which Equation (31) omits.
