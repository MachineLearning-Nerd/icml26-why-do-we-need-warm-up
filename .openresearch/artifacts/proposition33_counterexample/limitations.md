# Limitations and deviations

The counterexample falsifies the exact printed constants in Proposition 3.3(ii). It does not assert that CE plus L2 objectives lack all possible `(H0,H1)` constants, and it does not contradict the repaired constant containing `Clinear*f*`.
