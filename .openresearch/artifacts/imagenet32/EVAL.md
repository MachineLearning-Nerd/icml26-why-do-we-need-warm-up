# Evaluator contract

The command exits nonzero for cumulative regression, archive-integrity,
trajectory-completeness, or independent-reduction failure. The scientific
verdict is `VERIFIED` only if both exact architectures meet every predeclared
statistical criterion; otherwise it is honestly `BLOCKED`.
