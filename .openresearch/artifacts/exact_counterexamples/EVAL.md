# Evaluator contract

The fixed command exits nonzero if cumulative evidence regresses, symbolic and
autograd curvature disagree, a contradiction is non-strict, or a negative control
does not behave for its intended reason. Verdict scope is emitted beside every
counterexample.
