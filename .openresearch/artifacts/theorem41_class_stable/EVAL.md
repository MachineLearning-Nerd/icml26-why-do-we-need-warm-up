# Evaluation

The verifier exits nonzero unless the function is C2, all assumptions are explicit, the step satisfies Equation (63), the one-step hit is exact, and the displayed lower bound is strictly larger than one. A larger-epsilon target must remain noncontradictory.
