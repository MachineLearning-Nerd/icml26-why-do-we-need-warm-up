# Falsification source audit

- Paper: arXiv:2510.03164v2, source hashes recorded in the baseline audit.
- Proposition B.2: HTML anchor `#A2.Thmtheorem2`. It quantifies over two arbitrary
  `(H0,H1)`-smooth functions and supplies explicit constants for their sum.
- Theorem 4.3: HTML anchor `#S4.Thmtheorem3`. The statement assumes `(H0,H1)`-smoothness
  and `mu`-PL, but does not state `H1>0` or a small-`epsilon` domain.
- Historical proxy: judged Space revision `17e423af...`, embedded verifier accepts a finite
  log-log slope up to `1.2` as equivalent to a global affine curvature bound.

